import {FileText,LockKeyhole} from 'lucide-react';
const reports=[['Breast awareness summary','14 Sep 2026','Saved summary'],['Breast ultrasound report','02 Sep 2026','Patient confirmation pending']];
export default function Reports(){return <section className="max-w-4xl mx-auto"><span className="eyebrow">REPORT HISTORY</span><h1 className="text-4xl font-bold text-slate-800 mt-3">Your private timeline</h1><p className="text-slate-500 mt-2">A record of saved summaries and confirmed report details.</p><div className="mt-7 grid gap-3">{reports.map(([title,date,status])=><article className="glass-panel p-5 flex items-center gap-4" key={title}><span className="p-3 rounded-xl bg-sky-50 text-sky-700"><FileText/></span><div className="flex-1"><h2 className="font-semibold text-slate-800">{title}</h2><p className="text-sm text-slate-500">{date} · {status}</p></div><button className="secondary">Preview</button></article>)}</div><p className="text-xs text-slate-500 mt-5 flex gap-2"><LockKeyhole size={15}/> Previews remain inside Diagnex and do not provide medical interpretation.</p></section>}
