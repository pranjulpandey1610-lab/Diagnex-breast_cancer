"use client";
import {useState} from 'react'; import {FileUp,LockKeyhole,CheckCircle2,FileText,Edit3} from 'lucide-react';
const fields=[['BI-RADS category','Demo preview'],['Imaging modality','Breast ultrasound'],['Report date','02 Sep 2026'],['Breast side','Right'],['Lesion location','Upper area'],['Lesion size','Demo preview'],['Radiologist recommendation','Clinical follow-up may be appropriate']];
export default function Upload(){const [file,setFile]=useState('');const [stage,setStage]=useState<0|1|2>(0);return <section className="max-w-4xl mx-auto"><span className="eyebrow">PRIVATE REPORT ARCHIVE</span><h1 className="text-4xl font-bold text-slate-800 mt-3">Upload a breast-health report</h1><p className="text-slate-500 mt-2">PDF, JPG, and PNG only. Files are checked and stored privately before local extraction.</p><div className="glass-panel p-7 mt-7"><label className="block border-2 border-dashed border-sky-200 rounded-2xl p-12 text-center cursor-pointer bg-sky-50/30"><FileUp className="mx-auto mb-3 text-sky-600" size={32}/><b>{file||'Choose a report to upload'}</b><p className="text-sm text-slate-500 mt-2">Mammogram, ultrasound, MRI, pathology, biopsy, genetic, referral, or other clinical report</p><input className="hidden" type="file" accept=".pdf,.jpg,.jpeg,.png" onChange={e=>{setFile(e.target.files?.[0]?.name||'');setStage(1)}}/></label>{stage>0&&<div className="mt-6"><div className="flex justify-between text-sm"><span>Security validation and private storage</span><b>{stage===1?'Processing…':'Complete'}</b></div><div className="h-2 bg-slate-100 rounded-full mt-2"><div className={'h-2 bg-teal-500 rounded-full '+(stage===2?'w-full':'w-2/3')}/></div><button className="btn-primary mt-5" onClick={()=>setStage(2)}>Simulate secure processing</button></div>}</div>{stage===2&&<div className="glass-panel p-7 mt-5"><div className="flex gap-3"><CheckCircle2 className="text-teal-600"/><div><h2 className="font-semibold text-slate-800">Demo extraction preview</h2><p className="text-sm text-slate-500">Review and correct these fields before saving. Extracted text is not a diagnosis.</p></div></div><div className="mt-5 divide-y divide-slate-100">{fields.map(([key,value])=><div className="py-3 flex justify-between gap-5" key={key}><b className="text-sm text-slate-600">{key}</b><button className="text-sm text-sky-700 flex gap-1"><Edit3 size={14}/>{value}</button></div>)}</div><button className="btn-primary mt-5">Save confirmed report</button></div>}<p className="text-xs text-slate-500 mt-5 flex gap-2"><LockKeyhole size={15}/> No report text creates a diagnosis. New or persistent breast changes should be assessed by a qualified clinician.</p></section>}
